<nav class="navbar navbar-expand-lg navbar-light bg-light"  >
  <%= link_to "Contacts Manager", root_url, class: "navbar-brand", id:"logo"%>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto ">
     <li><span class="nav-link"> <%= link_to "Contacts", contacts_url %></span></li>
      <li class="nav-item">
         <li><span class="nav-link">  <%= link_to "About", welcome_about_url %></span></li>
      </li>
          <% if current_user.nil? %>
              <li class="nav-item">
                <li><span class="nav-link"><a href="<%= new_session_url %>">Sign In</a></span></li>
                <li><span class="nav-link"><a href="<%= new_user_url %>">Sign Up</a></li></span>
          <% else %>
                  <li><span class="nav-link"> Hello, <%= current_user.username %></span></li>
              </li>
                <li class="nav-item">
                  <span >
                    <form action="<%= session_url %>" method="post"  > 
                                    <input
                                    type="hidden"
                                    name="authenticity_token"
                                    value="<%= form_authenticity_token %>">
                                    <input type="hidden" name="_method" value="delete">
                                    <input type="submit" value="Logout" style="cursor:pointer; border:none; color: #007bff; background-color:#f8f9fa;" class="nav-link" >        
                    </form>
                  </span>
                </li>
            <% end %>
    </ul>
  </div>
</nav>


